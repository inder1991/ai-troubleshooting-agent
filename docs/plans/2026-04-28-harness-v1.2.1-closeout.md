# v1.2.1 Closeout — P2 backlog cleared

**Date:** 2026-04-28
**Scope:** All 9 P2 items (B19–B27) from
`docs/plans/2026-04-27-harness-sdet-audit.md`. This is polish only —
no consumer-visible breakage.

## What landed in v1.2.1

| Bug | Title | Files touched |
|-----|-------|---------------|
| B19 | refresh_baselines WARN on unparseable previous baseline | `tools/refresh_baselines.py` |
| B20 | load_harness surfaces OSError reads via malformed_files | `tools/load_harness.py` |
| B21 | extract.sh logs find-delete failures instead of swallowing | `tools/extraction/extract.sh` |
| B22 | setup_signing.sh `--protect` flag for human signers | `tools/setup_signing.sh` |
| B23 | install_pre_commit.sh falls back to python3 when make absent | `tools/install_pre_commit.sh` |
| B24 | load_harness uses fnmatchcase for cross-platform determinism | `tools/load_harness.py` |
| B25 | refresh_baselines refuses to overwrite baseline on buggy zero-finding run | `tools/refresh_baselines.py` |
| B26 | _session_start_hook.sh adds `set -o pipefail` | `tools/_session_start_hook.sh` |
| B27 | confirmed harness_policy_schema covers HARNESS_CARD.yaml (no change needed) | n/a |

Single squash commit. No new tests — these are sub-10-line fixes
where the existing test suite (325 passed / 2 skipped) is sufficient
to prove no regression.

## Verification

- `pytest tests/harness/ -q --ignore=tests/harness/fixtures` →
  **325 passed, 2 skipped** (unchanged from v1.2.0).
- `make validate-fast` → `VALIDATE_SUMMARY mode=fast status=PASS`.

## Audit ledger now fully closed

| Tier | Count | Status |
|------|-------|--------|
| P0 v1.0.x audit (B1–B6) | 6 | ✅ all closed in v1.1.0 |
| P0 v1.1.0 audit (B7–B10) | 4 | ✅ all closed in v1.1.1 |
| P1 v1.1.0 audit (B11–B18) | 8 | ✅ all closed in v1.2.0 |
| P2 v1.1.0 audit (B19–B27) | 9 | ✅ all closed in v1.2.1 |

**Total: 27 audit findings closed across four releases (v1.1.0,
v1.1.1, v1.2.0, v1.2.1).** A second-pass audit covering rule
semantics (vs. the orchestration / tooling / signing surface this
audit covered) would likely surface 10–20 more findings. Out of
scope unless explicitly requested.

## Outstanding (source-repo only)

Per the v1.1.1 and v1.2.0 closeouts, the source-side `ai-harness`
repo still needs to:

1. Bump `HARNESS_CARD.yaml.version` to `1.2.1` (in the source
   repo, not this consumer — the v1.2.1 commit that paired the
   bump-and-tag is the source-side commit).
2. Run `bash tools/extraction/extract.sh` (now smoke-tested by B17).
3. `git tag -s v1.2.1 -m "ai-harness v1.2.1 — P2 polish"`.
4. `git push origin main v1.2.1`.
5. `gh release create v1.2.1 --notes-file RELEASES.md`.

Consumers then bump `.harness-version` to `v1.2.1` and run
`python3 tools/sync_harness.py --trust-key 73A7AF8F04F40EC9`.
